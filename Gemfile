source "https://rubygems.org"

# Pinned: recent fastlane/cocoapods releases require Ruby >= 2.7/3.0.
# If `bundle install` fails to resolve or build native extensions on
# this machine's system Ruby, install a newer Ruby (rbenv/asdf) first
# rather than loosening these pins.
gem "fastlane", "~> 2.223"
gem "cocoapods", "~> 1.15"
